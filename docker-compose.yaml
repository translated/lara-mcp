services:
  lara-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    container_name: lara-mcp
    restart: unless-stopped
    working_dir: /opt
    command: ["pnpm", "start"]
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env